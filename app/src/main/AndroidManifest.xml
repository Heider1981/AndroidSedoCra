<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo02"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo02"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sedo_CRA"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity02"
            android:exported="true">
            <!-- Puedes agregar un intent-filter si quieres que esta actividad sea iniciada desde algún lugar -->
        </activity>

        <activity
            android:name=".MainActivity03"
            android:exported="true">
            <!-- Puedes agregar un intent-filter si quieres que esta actividad sea iniciada desde algún lugar -->
        </activity>

        <activity
            android:name=".MainActivity04"
            android:exported="true">
        </activity>

        <activity
            android:name=".MainActivity05"
            android:exported="true">
        </activity>

        <activity
            android:name=".MainActivity09"
            android:exported="true">
        </activity>

    </application>

</manifest>

